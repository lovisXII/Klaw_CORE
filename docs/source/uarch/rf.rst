Write Back
----------

There is actual write back stage (at least not a file), the data from exe is flopped then
sent to the register bank. Once the register bank (or register file, this is equivalent)
receive the data it is written in the corresponding register.